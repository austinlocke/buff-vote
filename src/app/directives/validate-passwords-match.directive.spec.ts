import { ValidatePasswordsMatchDirective } from './validate-passwords-match.directive';

describe('ValidatePasswordsMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidatePasswordsMatchDirective();
    expect(directive).toBeTruthy();
  });
});
